
1.20.1-1.3.2-beta-3

[✔] Done and tested
[🟡] Partially done and/or needs testing
[❌] Not done, including unmerged PRs


Scenario roles:
    [❌] Use start menu colours for each role
    [❌] When role is cycled, move camera to the first found building (or unit if no building)

[❌] Show different colours for each different assigned role
[❌] Fire events for addons to use when a scenario starts

[✔] Custom Building commands
    - Use EditBox from TextInputClientEvents
    - On commands page:
         - List of command string inputs (by default 1 empty)
         - On left of each, trigger condition
            - On built, on destroy, periodically while built (with extra timer button)
         - On right of each, delete button
         - Below last list, add new command line
    - Send packets to server
    - Save and load data

[🟡] rtsmap.json support
    - Read the rtsmap.json into a java class on map load
    - Write an /rts-set-team-mode command that can choose the mode available from map.json (or error if it wasn't specified)
    - If there is a map.json present and team mode chosen, use the given start positions and ignore start blocks

Bugfixes
--------
[✔] Fixed player selector not working for /rtsapi-set-camera

[✔] Workers no longer automatically gather wood from bridges


Quality of Life
---------------
[❌] Ready check instead of start game countdown

[❌] Ravager artillery now have an attack-ground button

[❌] Review packets that can be modified to only send to some players for performance, TRACKING_ENTITY for updating units only for players who have it loaded clientside

[✔] Added a "Nether terrain only" option to custom buildings

attack damage
attacks per second
aggro range
ranged armour %
magic armour %
movement speed

for heroes:
base max health
base max mana
mana regen per second
max health
attack damage bonus per level
max health bonus per level
max mana bonus per level

Balance
-------


Languages
---------